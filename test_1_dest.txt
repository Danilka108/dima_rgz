+
+
<
<
=
=
=
=
=
=
=
=
=
=
=
0)
0)
0)
!=
!=
+=
i
i
i
i
0;
0;
1;
{
{
{
{
{
{
}
}
}
}
}
};
1];
1];
if
if
i++)
i++)
'\0';
len
for
for
new
new
new
int
str
(len
(int
(int
len;
str;
_len
void
void
char*
char*
file,
_len;
file);
delete
return
struct
string)
string;
SEEK_SET);
strings
delete[]
delete[]
strings;
strings;
metadata,
*metadata,
fread(str,
fseek(file,
File_string
File_string*
File_string*
File_string*
sizeof(char),
new_strings
file_string)
metadata->len
new_strings;
(metadata->len
File_metadata
metadata->len;
File_metadata*
new_strings[i]
~File_metadata()
read_string(FILE*
file_string->len,
File_metadata(int
metadata->strings
metadata->strings;
File_string[_len];
file_string->offset,
additional_metadata)
additional_metadata;
metadata->strings[i];
char[file_string->len
push_string(metadata,
str[file_string->len]
additional_metadata->len;
File_string[metadata->len
push_string(File_metadata*
new_strings[metadata->len]
additional_metadata->strings[i]);
concat_files_metadata(File_metadata